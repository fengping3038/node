# 📦 电商管理系统 - 完整前后端项目

## 🎯 项目简介
这是一个完整的电商管理系统，包含：
- **前端**: Vue 3 + Vite + Element Plus + Pinia
- **后端**: Koa2 + MySQL + Sequelize + JWT

## ✨ 功能特性

### 用户模块
- ✅ 用户注册
- ✅ 用户登录（JWT认证）
- ✅ 密码加密存储（bcryptjs）

### 商品模块
- ✅ 商品列表展示
- ✅ 商品搜索和筛选
- ✅ 新增商品
- ✅ 编辑商品
- ✅ 删除商品
- ✅ 库存统计

## 🛠️ 技术栈

### 前端
- Vue 3.5
- Vite 5.4
- Element Plus 2.14
- Pinia 4.0
- Vue Router 4.6
- Axios

### 后端
- Koa 2.15
- Koa Router
- MySQL (通过 Sequelize ORM)
- JWT (身份认证)
- bcryptjs (密码加密)
- dotenv (环境变量)

## 📁 项目结构

```
node/
├── koa2/                    # 后端项目
│   ├── config/
│   │   └── db.js           # 数据库配置
│   ├── controllers/
│   │   ├── userController.js    # 用户控制器
│   │   └── productController.js # 商品控制器
│   ├── middleware/
│   │   └── auth.js         # JWT认证中间件
│   ├── models/
│   │   ├── User.js         # 用户模型
│   │   └── Product.js      # 商品模型
│   ├── routes/
│   │   ├── userRoutes.js   # 用户路由
│   │   └── productRoutes.js # 商品路由
│   ├── .env                # 环境变量配置
│   ├── .gitignore
│   ├── app.js              # 主应用入口
│   ├── initDB.js           # 数据库初始化脚本
│   ├── package.json
│   ├── README.md           # API文档
│   └── STARTUP.md          # 启动指南
│
└── web/                    # 前端项目
    ├── src/
    │   ├── router/         # 路由配置
    │   ├── stores/         # Pinia状态管理
    │   │   ├── user.js     # 用户store
    │   │   └── product.js  # 商品store
    │   ├── utils/
    │   │   └── api.js      # API请求封装
    │   ├── views/          # 页面组件
    │   │   ├── Login.vue   # 登录页
    │   │   ├── Register.vue # 注册页
    │   │   └── Products.vue # 商品管理页
    │   ├── App.vue
    │   └── main.js
    ├── index.html
    ├── package.json
    └── vite.config.js
```

## 🚀 快速开始

详细启动步骤请查看 [koa2/STARTUP.md](./koa2/STARTUP.md)

### 简要步骤：

1. **创建数据库**
   ```sql
   CREATE DATABASE nuxt_app CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. **初始化后端数据**
   ```bash
   cd koa2
   npm run init-db
   ```

3. **启动后端服务**
   ```bash
   npm run dev
   # 运行在 http://localhost:3000
   ```

4. **启动前端服务**
   ```bash
   cd ../web
   npm run dev
   # 运行在 http://localhost:5173
   ```

5. **测试登录**
   - 用户名: `admin`
   - 密码: `123456`

## 🔌 API 接口

### 用户接口
- `POST /api/register` - 用户注册
- `POST /api/login` - 用户登录

### 商品接口
- `GET /api/products` - 获取所有商品（公开）
- `GET /api/products/:id` - 获取单个商品（公开）
- `POST /api/products` - 创建商品（需认证）
- `PUT /api/products/:id` - 更新商品（需认证）
- `DELETE /api/products/:id` - 删除商品（需认证）

详细API文档请查看 [koa2/README.md](./koa2/README.md)

## 🔐 安全特性

- ✅ JWT Token 认证
- ✅ 密码 bcrypt 加密
- ✅ CORS 跨域支持
- ✅ 请求参数验证
- ✅ 错误统一处理

## 📝 开发说明

### 前端数据流
1. 组件调用 Store 方法
2. Store 通过 Axios 调用后端 API
3. API 返回数据更新 Store
4. 组件从 Store 获取响应式数据

### 后端架构
1. 路由接收请求
2. 中间件进行认证验证
3. 控制器处理业务逻辑
4. 模型层操作数据库
5. 返回统一格式的响应

## 🐛 常见问题

请参考 [koa2/STARTUP.md](./koa2/STARTUP.md) 中的常见问题部分。

## 📄 许可证
ISC

## 👥 作者
Generated by AI Assistant

---

**祝你使用愉快！** 🎉
